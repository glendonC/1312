# dependencies
node_modules/

# build
dist/
.astro/

# env / local
.env
.env.*
!.env.example
.studio/

# the wav is what the recogniser reads on this machine, not something the site serves.
# the mp4 and the waveform ship; the working track does not.
public/demo/runs/*/clip.wav

# OS / editor
.DS_Store
*.log
.vscode/
.idea/
