---
interface Props {
  index: string;
  label: string;
  title: string;
  description: string;
}

const { index, label, title, description } = Astro.props;
---

<header class="panel-heading">
  <div>
    <p>{index} / {label}</p>
    <h2>{title}</h2>
  </div>
  <p>{description}</p>
</header>
