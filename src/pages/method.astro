---
import GlassNav from "../components/GlassNav.astro";
import MethodProcess from "../features/method/MethodProcess";
import SiteLayout from "../layouts/SiteLayout.astro";
// Direct leaf imports so Vite invalidates each sheet; a CSS @import barrel can serve stale CSS until HMR.
import "../features/method/styles/layout.css";
import "../features/method/styles/cards.css";
import "../features/method/styles/responsive.css";
---

<SiteLayout
  title="Method | 1321"
  description="How 1321 gives autonomous agents inspectable workspaces for investigating real media, reconciling evidence, and producing useful outputs."
>
  <GlassNav />
  <main class="method-page">
    <MethodProcess />
  </main>
</SiteLayout>

<script>
  import { enhanceMethodProcess } from "../features/method/enhanceMethodProcess";

  let teardownMethodProcess = () => {};

  const setupMethodProcess = () => {
    teardownMethodProcess();

    const root = document.querySelector<HTMLElement>("[data-method-process]");
    teardownMethodProcess = root ? enhanceMethodProcess(root) : () => {};
  };

  document.addEventListener("astro:page-load", setupMethodProcess);
  setupMethodProcess();
</script>
