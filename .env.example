# Copy to .env (gitignored). Never commit real keys.
#
# Not required for: npm run dev, Explore a recording, or default
# npm run runtime:host YouTube/owned-file ingest (deterministic executor).
#
# Required for real OpenAI-backed paths (captions, language explanation,
# learning prep, and other --allow-real-* host flags):

OPENAI_API_KEY=
